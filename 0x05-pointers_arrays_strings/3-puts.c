#include "holberton.h"

/**
 * _puts - print a given string followed by a new line
 * @str: string to print
 *
 */

void _puts(char *str)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		_putchar(s[i]);
	}

	_putchar('\n');
}
