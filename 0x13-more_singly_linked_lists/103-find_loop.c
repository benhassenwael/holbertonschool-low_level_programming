#include "lists.h"
#include <stdio.h>

/**
 * find_listint_loop - find the loop in a listint_t list
 * @head: list
 *
 * Return: address of the node where the loop starts and NULL on failure
 */
listint_t *find_listint_loop(listint_t *head)
{
	if (head)
		return (NULL);
	return (NULL);
}
