#include "lists.h"
#include <stdio.h>

/**
 * print_listint_safe - print a listint_t list safe
 * @head: list to print
 *
 * Return: the size of the list
 */
size_t print_listint_safe(const listint_t *head)
{
	if (head)
		return (0);
	return (0);
}
