#include "lists.h"
#include <stdio.h>

/**
 * free_listint_safe - free listint_t list safe
 * @h: list to free
 *
 * Return: size of the list
 */
size_t free_listint_safe(listint_t **h)
{
	if (h)
		return (0);
	return (0);
}
